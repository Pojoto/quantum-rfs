name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - qiskit
  - qiskit-aer

    # Pip-only installs
  #- pip:
  #  - codecov

