name: docs_quantum_rfs
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton

  - qiskit
  - qiskit-aer


    # Pip-only installs
  - pip:
    - -e ../

